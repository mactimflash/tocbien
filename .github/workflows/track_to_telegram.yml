- name: Send Telegram
  run: |
    curl -X POST "https://api.telegram.org/bot${{ secrets.TELE_TOKEN }}/sendMessage" \
      -d chat_id=${{ secrets.TELE_CHAT_ID }} \
      -d text="🔥 New Visitor:
Path: ${{ github.event.client_payload.path }}
Ref: ${{ github.event.client_payload.ref }}
UA: ${{ github.event.client_payload.ua }}"
